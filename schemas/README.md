# UCM Schemas

Machine-readable schema files for UCM vNext.

- `role-contract.schema.json` validates `roles/contracts/*.yaml` after YAML-to-JSON loading.
- `artifacts/*.schema.json` validates core artifact payloads.

These schemas are starter contracts for runtime validation. They are not wired into the desktop runtime yet.
