{
  "name": "ucm",
  "version": "0.3.0",
  "private": true,
  "description": "UCM desktop-first workspace",
  "scripts": {
    "desktop:dev": "cd ucm-desktop && npm run dev",
    "desktop:build": "cd ucm-desktop && npm run build",
    "desktop:typecheck": "cd ucm-desktop && npm run typecheck",
    "desktop:test:runtime": "cd ucm-desktop && npm run test:runtime",
    "desktop:test:smoke": "cd ucm-desktop && npm run test:smoke"
  }
}
